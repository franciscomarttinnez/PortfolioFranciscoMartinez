import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://franciscomarttinnez.github.io",
  base: "/PortfolioFranciscoMartinez/",
  outDir: "docs",
  output: "static"
});
